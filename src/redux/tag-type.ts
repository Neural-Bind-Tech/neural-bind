export enum tagTypes {
    settings = 'settings',
    generalSettings = 'generalSetting',
    patient = 'patient',
    doctor = 'doctor',
    item = 'item',
    medicine = 'medicine',
}

export const tagTypesList = [
    tagTypes.settings,
    tagTypes.generalSettings,
    tagTypes.patient,
    tagTypes.doctor,
    tagTypes.item,
    tagTypes.medicine
]